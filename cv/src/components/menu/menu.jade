link(rel="import" href="../3rdparty/polymer.html")
link(rel="import" href="../pathBehavior/pathBehavior.html")
  
dom-module#comp-menu
  template
    style !{readFile('menu.css')}
    
    .cv CV
    .person
      p {{ data.name }}
      p {{ data.position }}
    
    .items
      template(is="dom-repeat" items="{{ items }}")
        a(href$="{{ item.url }}" class$="{{ itemClass(path, item.url) }}") {{ item.name }}

  script !{readFile('menu.js')}
